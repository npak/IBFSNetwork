@model IBFSNetwork.Models.ForumViewModels.SubThread

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit SubForum</h2>

<form asp-action="Edit">
    <div class="form-horizontal">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <input type="hidden" asp-for="SubThreadId" />
        <div class="form-group">
            <label class="control-label col-md-2">Forum</label>
            <div class="col-md-10">
                <select asp-for="ForumId" class="form-control" asp-items="ViewBag.ForumId"></select>
                <span asp-validation-for="ForumId" class="text-danger" />
            </div>
        </div>
        <div class="form-group">
            <label asp-for="Title" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <input asp-for="Title" class="form-control" />
                <span asp-validation-for="Title" class="text-danger" />
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
